version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: sentinel_core_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: sentinel_core_user
      POSTGRES_PASSWORD: sentinel_core_pass
      POSTGRES_DB: sentinel_core_db
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: sentinel_core_redis
    restart: unless-stopped
    ports:
      - '6379:6379'

  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    container_name: sentinel_core_api
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://sentinel_core_user:sentinel_core_pass@postgres:5432/sentinel_core_db
      JWT_SECRET: your-jwt-secret-change-in-production
      JWT_REFRESH_SECRET: your-refresh-secret-change-in-production
      REDIS_URL: redis://redis:6379
    ports:
      - '3001:3001'
    depends_on:
      - postgres
      - redis

volumes:
  postgres_data:
